// Re-export placement components from their canonical locations
// These barrel exports keep the module self-contained while
// the source files remain in their original paths (no breaking changes).

export { PlacementErrorBoundary } from '@/components/placement/PlacementErrorBoundary';
